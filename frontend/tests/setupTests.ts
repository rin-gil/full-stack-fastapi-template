/**
 * @file Test setup file for Vitest.
 * This file is executed once before all tests run to set up the testing environment.
 * It imports `@testing-library/jest-dom` to extend Vitest's `expect` matchers
 * with custom DOM matchers, allowing for more declarative assertions on the DOM.
 */

import "@testing-library/jest-dom"
